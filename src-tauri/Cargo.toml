[package]
name = "technoscope-desktop"
version = "1.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["shell-open-api", "dialog-open", "dialog-save", "fs-scope"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
